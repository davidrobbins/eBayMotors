var make = ds.Make.find("title = :1", "Buick");make //.modelCollection.remove()//Create some  Models./**/for (var i = 0; i < 17; i++) {	var newModel = new ds.Model({		title: "Buick" + "Model" + i,		make: make	});		newModel.save()		//Create some variants of the this Model.	for (var j = 0; j < 12; j++) {		var newVariant = new ds.Variant({			title: newModel.title + "v" + j,			model: newModel		});				newVariant.save();				//Create some new AutoB vehicles.		for (var k = 0; k < 9; k++) {			new ds.Vehicle({				name: newVariant.title + k,				engineDisplacement: 1596,				horsePower: 102,				variant: newVariant			}).save();		} //end - for (var k = 0; k < 9; k++)			} //end - for (var j = 0; j < 12; j++) {} //end - for (var i = 0; i < 17; i++)